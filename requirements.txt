# Replication package -- Python dependencies (tested with Python 3.12).
# Pin these for exact reproduction; newer minor versions are expected to work.
numpy>=1.26,<3
scipy>=1.11          # replicate_section6.py (sweep): Brown-Forsythe F p-value
numba>=0.59          # JIT kernels in mlb_core.py (compiled once, cached)
matplotlib>=3.7      # figures (Sections 4 and 6)
joblib>=1.3          # parallel Monte Carlo (calibration and robustness)
# No pandas / openpyxl required: all I/O is via the csv standard library.
